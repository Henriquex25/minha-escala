import { Text, View } from 'react-native'
import Body from '../components/layout/Body'

export default function GenerateScaleScreen() {
    return (
        <Body>
            <View>
                <Text className="bg-red-500">Gerar Escala</Text>
            </View>
        </Body>
    )
}