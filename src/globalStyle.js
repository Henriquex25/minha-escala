import { StyleSheet } from "react-native";

export const globalStyle = StyleSheet.create({
    text: {
        color: "#e5e7eb",
    },
    title: {
        textAlign: "center",
        fontWeight: "700",
        color: "#e5e7eb",
    },
});
